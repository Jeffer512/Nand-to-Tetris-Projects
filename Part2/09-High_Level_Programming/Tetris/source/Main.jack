class Main {

    /** Initializes a Tetris game and starts running it. */
    function void main() {
        var Tetris game;
        do Tetris.newInstance(11, 16, 511, 1);
        let game = Tetris.getInstance();
        do game.run();
        do game.dispose();
        return;
    }
} 